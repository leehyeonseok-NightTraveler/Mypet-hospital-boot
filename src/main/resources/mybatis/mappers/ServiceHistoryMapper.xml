<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.ServiceHistoryDAO">

    <select id="getServiceHistory"
            parameterType="int"
            resultType="com.boot.dto.ServiceHistoryDTO">
        SELECT
            service_no,
            user_no,
            service_date,
            service_type,
            service_item,
            details_memo,
            completion_status,
            completion_date
        FROM service_history
        WHERE user_no = #{user_no}
        ORDER BY service_date DESC
    </select>

</mapper>
